import Root, {
  ButtonProps,
  ButtonSize,
  ButtonVariant,
  buttonVariants,
} from './button.svelte';

export {
  type ButtonProps,
  type ButtonProps as Props,
  type ButtonSize,
  type ButtonVariant,
  buttonVariants,
  Root,
  //
  Root as Button,
};
